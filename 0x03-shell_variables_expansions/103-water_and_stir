#!/bin/bash

# Retrieve the values from the environment variables
water=$WATER
stir=$STIR

# Perform the addition and convert the result to base bestchol
result=$(echo "ibase=base_water; obase=base_bestchol; $water + $stir" | bc)

# Print the result
echo "$result"
